so far, tried naive aproach using claude api, now using local models ad tracking variables to try and understand lurkers and predict based off 
of a seed tweet

test starting with just a tweet and with a thread already populated

look at how polarised twitter users can affect the entire population

look at the impact of the first response in a tweet

try running on gpu with different tweets and with different parameters.

llms are non deterministic, how can we rely on it when the output is different every time even with the same input